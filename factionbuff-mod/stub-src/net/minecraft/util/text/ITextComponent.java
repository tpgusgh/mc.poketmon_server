package net.minecraft.util.text;

/**
 * Compile-only stub -- see IFormattableTextComponent.java in this same tree
 * for why (javac's class-file reader rejects the real srg jar's copy of
 * these text classes with a "bad RuntimeInvisibleParameterAnnotations
 * attribute" error, even though the JVM loads them fine at runtime). Never
 * bundle this stub into the mod jar.
 */
public interface ITextComponent {
}
